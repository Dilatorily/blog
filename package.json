{
  "name": "fictional-octo-spork",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean": "rimraf public",
    "prebuild": "npm run clean",
    "build": "ncp src public --filter=\\.\\(\\\\.html\\|\\\\.ico\\)?$ && babel src -d public",
    "start": "http-server"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "http-server": "^0.9.0",
    "ncp": "^2.0.0",
    "rimraf": "^2.5.2"
  }
}
