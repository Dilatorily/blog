{
  "name": "fictional-octo-spork",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean": "rimraf public",
    "prebuild": "npm run clean",
    "build": "ncp src public",
    "start": "http-server"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "ncp": "^2.0.0",
    "rimraf": "^2.5.2"
  }
}
