import * as Style from '../style';

describe('style', () => {
  it('should export LIGHT_RED', () => {
    expect(Style.LIGHT_RED).toBeDefined();
  });

  it('should export RED', () => {
    expect(Style.RED).toBeDefined();
  });

  it('should export LIGHT_BLUE', () => {
    expect(Style.LIGHT_BLUE).toBeDefined();
  });

  it('should export DARK_BLUE', () => {
    expect(Style.DARK_BLUE).toBeDefined();
  });

  it('should export DARKER_BLUE', () => {
    expect(Style.DARKER_BLUE).toBeDefined();
  });

  it('should export LIGHT_TEAL', () => {
    expect(Style.LIGHT_TEAL).toBeDefined();
  });

  it('should export DARK_TEAL', () => {
    expect(Style.DARK_TEAL).toBeDefined();
  });

  it('should export LIGHT_GREEN', () => {
    expect(Style.LIGHT_GREEN).toBeDefined();
  });

  it('should export DARK_GREEN', () => {
    expect(Style.DARK_GREEN).toBeDefined();
  });

  it('should export PURPLE', () => {
    expect(Style.PURPLE).toBeDefined();
  });

  it('should export WHITE', () => {
    expect(Style.WHITE).toBeDefined();
  });

  it('should export LIGHT_GREY', () => {
    expect(Style.LIGHT_GREY).toBeDefined();
  });

  it('should export NEUTRAL_GREY', () => {
    expect(Style.NEUTRAL_GREY).toBeDefined();
  });

  it('should export DARK_GREY', () => {
    expect(Style.DARK_GREY).toBeDefined();
  });
});
