import styled from '@emotion/styled';

const Preview = styled.p`
  margin: 0;
  font-size: 18px;
  font-weight: 200;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;

export default Preview;
