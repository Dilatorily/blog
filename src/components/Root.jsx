import React from 'react';
import { StyleRoot } from 'radium';
import RootRouter from './RootRouter';

const Root = () => (
    <StyleRoot>
        <RootRouter />
    </StyleRoot>
);

export default Root;
