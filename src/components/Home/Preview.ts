import { styled } from '@linaria/react';

const Preview = styled.p`
  font-size: 18px;
  font-weight: 200;
  margin: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;

export default Preview;
