import { styled } from '@linaria/react';
import { Color } from '../../constants';

const Link = styled.a`
  color: ${Color.DarkGrey};
  display: block;
  text-decoration: none;
`;

export default Link;
